# Bayesian Probability

This directory contains tasks for Bayesian probability.

## Task 0

- `0-likelihood.py`: computes the likelihood of observing `x` successes in
  `n` Bernoulli trials for each hypothetical probability in `P`.
Probability

This project implements common probability distributions from scratch in
Python, without using any external modules. Each distribution is built as a
class that can either take a list of data to estimate its parameters, or take
the parameters directly.

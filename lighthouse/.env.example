# Optional. Add a key to enable the AI helpers (auto-breakdown + tone rephrasing).
# The app works fully without it. This key is read ONLY by the local proxy
# (server/proxy.js) and never reaches the browser.
ANTHROPIC_API_KEY=
# Optional model + port overrides
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
PROXY_PORT=8787
